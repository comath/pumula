<%= form_for(@assessment_weight) do |f| %>
  <% if @assessment_weight.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@assessment_weight.errors.count, "error") %> prohibited this assessment_weight from being saved:</h2>

      <ul>
      <% @assessment_weight.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
